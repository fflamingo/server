{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "test:ci":
      "lerna run test -- --coverage --ci --testResultsProcessor=\"jest-junit\"",
    "coverage": "lerna run coverage"
  },
  "workspaces": ["packages/*"],
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
