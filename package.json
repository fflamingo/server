{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "jest",
    "test:ci": "jest --coverage --ci --testResultsProcessor=\"jest-junit\"",
    "typings": "lerna exec --stream tsc -- --noEmit",
    "coverage": "codecov"
  },
  "workspaces": ["packages/*"],
  "devDependencies": {
    "lerna": "^8.0.0"
  }
}
