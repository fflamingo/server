{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "jest",
    "test:ci": "jest --coverage --ci --testResultsProcessor=\"jest-junit\"",
    "coverage": "codecov"
  },
  "workspaces": ["packages/*"],
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
